import { OnInit, EventEmitter, OnDestroy } from '@angular/core';
import { IntlService, LangChangeEvent, FormatService } from '../services';
export declare abstract class AbstractI18nComponent implements OnInit, OnDestroy {
    protected intlService: IntlService;
    protected formatService: FormatService;
    result: string;
    onLangChange: undefined | EventEmitter<LangChangeEvent>;
    constructor(intlService: IntlService, formatService: FormatService);
    abstract updateValue(): void;
    ngOnInit(): void;
    /**
    * Clean any existing subscription to change events
    * @private
    */
    _dispose(): void;
    ngOnDestroy(): void;
}
