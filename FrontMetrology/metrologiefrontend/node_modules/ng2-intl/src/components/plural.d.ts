import { OnInit, OnDestroy, OnChanges, SimpleChanges } from '@angular/core';
import { IntlService, FormatService } from '../services';
import { AbstractI18nComponent } from './abstractI18n';
export declare class FormattedPluralComponent extends AbstractI18nComponent implements OnInit, OnDestroy, OnChanges {
    value: string;
    options: Object;
    other: string;
    zero?: string;
    one?: string;
    two?: string;
    few?: string;
    many?: string;
    constructor(intlService: IntlService, formatService: FormatService);
    updateValue(): void;
    ngOnChanges(changes: SimpleChanges): void;
}
