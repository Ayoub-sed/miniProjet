import { Http } from '@angular/http';
import { Observable } from 'rxjs/Observable';
export declare abstract class IntlLoader {
    abstract getMessages(lang: string): Observable<any>;
}
export declare class IntlStaticLoader implements IntlLoader {
    private http;
    private prefix;
    private suffix;
    constructor(http: Http, prefix?: string, suffix?: string);
    /**
     * Gets the messages from the server
     * @param lang
     * @returns {any}
     */
    getMessages(lang: string): Observable<any>;
}
